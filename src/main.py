from tests.run_experiments import *

if __name__ == "__main__":
    # run_example_graph()
    # run_energy_landscape_regular_graph(ising=True)
    # run_energy_landscape_regular_graph(ising=False)
    test_example_scale_free_graph(50, 2.4, 5)
